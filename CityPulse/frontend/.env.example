# Copy to frontend/.env for local dev, or set these as Vercel env vars for deploy.
VITE_API_URL=http://127.0.0.1:8000
VITE_WS_URL=ws://127.0.0.1:8000/ws/citypulse
# Production example (after Render deploy — replace the host):
# VITE_API_URL=https://citypulse-backend.onrender.com
# VITE_WS_URL=wss://citypulse-backend.onrender.com/ws/citypulse
